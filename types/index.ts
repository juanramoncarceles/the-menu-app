export interface ItemData {
  id: string;
  title: string;
  price: number;
  description: string;
}

export interface CategoryData {
  id: string;
  name: string;
}

export interface OrderItem {
  id: string,
  qty: number
}